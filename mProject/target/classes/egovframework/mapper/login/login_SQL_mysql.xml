<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	
	<resultMap id="user" type="egovframework.example.login.vo.userVo">
		<result property="user_id" column="user_id"/>
		<result property="user_pswd" column="user_pswd"/>
		<result property="state" column="state"/>
		<result property="user_name" column="user_name"/>
		
	</resultMap>
	<!-- insert user -->
	<insert id="insertMember">
		INSERT INTO 
		USER(
			user_id
			, user_pswd
			, state
			, user_name
		)VALUES(
			#{user_id}
			, #{user_pswd}
			, 'n'
			, #{user_name}
		 );
	</insert>
	
	 <!-- login -->
	 <select id="userLogin" resultType="egovframework.example.login.vo.userVo">
	 	SELECT
	 		user_id
	 		, user_pswd
	 		, state
	 		, user_name
	 	FROM
	 		user
	 	WHERE
			user_id = #{user_id} 

	 </select>
	 
	 <!-- 아이디중복확인 -->
	 <select id="idOverlap" resultType="String" parameterType="egovframework.example.login.vo.userVo">
	 	SELECT
	 		user_id
	 	FROM
	 		user
	 	WHERE
	 		user_id = #{user_id}
	 </select>

</mapper>
